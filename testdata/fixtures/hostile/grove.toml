# Deliberately unfinished, the way `grove init` leaves it.

[project]
name = "hostile"
default_branch = "master"

[compose]
files = ["docker-compose.yml"]

[[service]]
name = "web"
port = 8080
default = true

# This service does not exist in the compose file.
[[service]]
name = "worker"
port = 9000

[[service]]
name = "db"
port = 5432
tcp = true

[[stateful]]
service = "db"
# The volume key is "pgdata"; this names something else.
volume = "database"
inputs = []

  [stateful.prepare]
  mode = "run"
  service = "web"
  command = []
